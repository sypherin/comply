# --- local demo/offline settings ---
AUTH_MODE=demo          # demo | msal | easy_auth
OFFLINE_MODE=true       # true => Graph is a no-op; emails simulated; no tokens; no HTTP
USE_AZURE_SQL=false     # in-memory only
